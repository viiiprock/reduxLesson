# Phần 1: Bắt đầu từ Mock Up

Tôi tạo một mockup bằng React để hình dung mọi thứ rõ ràng hơn

```

//Tạo một Form 

const AddTodo = ({onSubmit, onClick, children}) => {
  
  return (
    <form onSubmit={onSubmit}>
      <input type="text"/>
      <button type="button" onClick={onClick}>
        {children}
      </button>
    </form>
  );
}

// Danh sách todo task đã tạo ra  
const TodoList = () => {
  
  return (
    <ul>
      <li>Item 1</li>
      <li>Item 1</li>
    </ul>
  )
}

//Đây là menu để lọc các nội dung đã hoàn thành hay chưa hoàn thành 
const Links = (props) => {
  
    return (
      <div>
        Show: 
        {"  "}
        <a href="#">All</a>
        {" . "}
        <a href="#">Active</a>
        {" . "}
        <a href="#">Completed</a>
      </div>
    )
}

class App extends React.Component {
  
  render() {
    return (
      <div>
        <AddTodo>
          Add Task
        </AddTodo>
        <TodoList/>
        <Links/>
      </div>
    );
  }

}

ReactDOM.render(
  <App/>,
  document.getElementById('app')
);
```